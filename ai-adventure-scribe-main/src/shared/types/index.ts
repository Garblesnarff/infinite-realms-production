/**
 * Shared Types Index
 *
 * Common TypeScript types and interfaces used across multiple features.
 * Feature-specific types should remain in their feature directories.
 *
 * Examples of what should go here:
 * - common.ts (generic utility types)
 * - api.ts (API request/response types)
 * - database.ts (database model types)
 */

// Placeholder for future shared types

export {};
