/**
 * Shared Utilities Index
 *
 * Generic utility functions that are used across multiple features.
 * Feature-specific utilities should remain in their feature directories.
 *
 * Examples of what should go here:
 * - formatting.ts (date formatting, number formatting, etc.)
 * - validation.ts (generic validators)
 * - array-helpers.ts (generic array operations)
 * - string-helpers.ts (generic string operations)
 */

// Placeholder for future generic utilities

export {};
