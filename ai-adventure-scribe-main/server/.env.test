# Test environment variables
# These are placeholder values to allow tests to load
# Set DATABASE_URL to a real database connection to run integration tests

NODE_ENV=test
DATABASE_URL=

# WorkOS AuthKit (Test)
WORKOS_API_KEY=test-workos-api-key
WORKOS_CLIENT_ID=test-workos-client-id
WORKOS_REDIRECT_URI=http://localhost:3000/auth/callback

# Legacy Supabase (for database/storage only)
SUPABASE_URL=https://placeholder.supabase.co
SUPABASE_ANON_KEY=placeholder-anon-key
SUPABASE_SERVICE_ROLE_KEY=placeholder-service-role-key
JWT_SECRET=test-jwt-secret-change-me-in-production

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:5173
